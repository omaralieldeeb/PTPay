import 'package:flutter/material.dart';
main() => runApp(const PTPayApp());
class PTPayApp extends StatelessWidget {
  const PTPayApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: null,
    );
  }
}
